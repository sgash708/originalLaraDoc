{
  "executionRoleArn": "arn:aws:iam:999999999999:role/ecsTaskExecutionRole",
  "containerDefinition": [
    {
      "entryPoint": ["sh", "-c"],
      "portMappings": [
        {
	  "hostPort": 80,
	  "protocol": "tcp",
	  "containerPort": 80
	}
      ],
      "command": ["npm ci && npm run prod && composer install --no-dev && composer dump-autoload && php artisan storrage:link && chown -R www-data:www-data /var/www/html/storage && php artisan config:cache && php artisan route:cache && /usr/sbin/apache2ctl -D FOREGROUND"],
      "taskDefinitionArn": "arn:aws:ecs:ap-northeast-1:999999999999:task-definition/project-name:1",
      "family": "project-name",
      "workingDirectory": "/var/www/html",
      "environment": []
    }
  ]
}
